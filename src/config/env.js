// In dev mode, Vite proxy forwards /api/* requests to the backend,
// so API_BASE_URL is empty (same origin). No CORS issues.
// In production, set VITE_API_BASE_URL on your deployment platform
// (e.g. VITE_API_BASE_URL=https://your-domain.com).


export const API_BASE_URL='https://e-commerce-backend-api-1slf.onrender.com';
